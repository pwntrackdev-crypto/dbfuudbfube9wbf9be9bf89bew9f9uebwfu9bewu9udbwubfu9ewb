# Netlify configuration for STREAMING version

# Edge Function configuration
[[edge_functions]]
  path = "/api/chat-stream"
  function = "chat-stream"

# Build settings (optional)
[build]
  publish = "."
