[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Increase timeout for long code generation
[[functions]]
  [functions.chat]
    timeout = 60  # 60 seconds max (Netlify free tier limit)
